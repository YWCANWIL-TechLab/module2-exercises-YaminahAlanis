// * Assignment Operators

// TODO Given variables first and second, increase the value of first by the value of second using +=
let first = 20;
let second = 30;

// TODO Given variables num1 and num2, decrease the value of num1 by the value of num2 using -=
let num1 = 100;
let num2 = 99;

// TODO Given variables var1 and var 2, multiply the value of var1 by the value of var2 using *=
let var1 = 5;
let var2 = 10;

// TODO Given variables x and y, divide x by the value of y using /=
let x = 1000;
let y = 20;

// TODO Given variables a and b, modulus divide a by the value of b using %=
let a = 10;
let b = 2;

// TODO Given variables base and exponent, the base to the power of exponent using **=
let base = 5;
let exponent = 5;

// * String Operators

// TODO Add the following strings together
let string1 = "Hello";
let string2 = "World";

// TODO Add the following strings
let string3 = "What happens if a add a number to a string? "
let number1 = 1;

// ? What happens to a variable after multiple reassignments?
let multiString = "What's going to happen";
let newString = "howdy";
multiString = newString;
newString = "Hmmm. What do you think will happen to the value of multiString?";
console.log(multiString);